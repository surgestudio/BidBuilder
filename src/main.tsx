import React from 'react'
import ReactDOM from 'react-dom/client'
import BidBuilder from './BidBuilder.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BidBuilder />
  </React.StrictMode>,
)